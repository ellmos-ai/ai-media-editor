# --- Private / user-specific (NEVER commit) ---
# real host/user/ssh/paths/tokens — use settings.example.json instead
config/settings.json
# machine-specific install snapshot (kept as a local reference only)
docs/OPENMONTAGE-KOMPONENTEN.md
docs/OPENMONTAGE-KOMPONENTEN.pdf
/_reports/

# --- Content / projects (personal media) ---
/projects/*
!/projects/.gitkeep

# --- Media ---
*.wav
*.mp3
*.mp4
*.mov
*.m4a
*.png
*.jpg
*.jpeg

# --- Python / build ---
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
venv/
.env
*.db
build/
dist/
*.egg-info/
wheelhouse/
.wheel-smoke/

# --- Multi-Host Sync Conflicts & Multi-Agent Locks ---
*.sync-conflict-*
*.conflict
*-CONFLIT-*
*-conflict-*
*.sync-temp-*
*-ASUS-GEI*
*-WORKSTATION*
* (kopie)*
* (Kopie)*
* (copy)*
* (Copy)*
*conflicted copy*
LOCK
LOCK.*
*.lock
LOCK*.txt
LOCK.permissions.json
uv.lock
!package-lock.json

# --- Temporary & Backup Files ---
*.tmp
*.bak
*.swp
*~
*.log
*.orig
*.rej

# --- Caches & Test Artifacts ---
.mypy_cache/
.tox/
.turbo/

# --- Data ---
data/

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
